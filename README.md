# Minimal config

This config only contains basic stuff 

Neovim, Tmux and Alacritty config. `neovim` is already uploaded to a separate repo
So is `tmux`!

Designed to be deployed on remote servers, or inside distrobox.
In both of these cases, the GUI is either native (Windows, GNOME) or totally not needed (SSH)
